<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Hierarchy_Tier__c</fullName>
    <description>Account Hierarchy Tier of the current Account record (6 Levels)</description>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(Parent.Parent.Parent.Parent.Parent.Name)), 6,
IF(NOT(ISBLANK(Parent.Parent.Parent.Parent.Name)), 5,
IF(NOT(ISBLANK(Parent.Parent.Parent.Name)), 4,
IF(NOT(ISBLANK(Parent.Parent.Name)), 3,
IF(NOT(ISBLANK(Parent.Name)),2,
1)))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Account Hierarchy Tier</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
